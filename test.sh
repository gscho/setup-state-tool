#!/bin/bash
installArgs=""
      
        installArgs="-v lol "
      
        installArgs="${installArgs}-b beta"
        echo $installArgs
      
# sh <(curl -q https://platform.activestate.com/dl/cli/install.sh) $installArgs